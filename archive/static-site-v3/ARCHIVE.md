Snapshot of the Astro + vanilla-Three.js interactive WebGL hero build (globe + flanking wedges + orbit rings composition, uncommitted final tuning included) before the rebuild of the hero as a particle-stag scene on React Three Fiber. Plain copy of the working tree's site-relevant source (`src/`, `public/`, config files) as it stood at commit `69f9fb0` ("Fix deploy Node version, animate block colors as an RGB gradient") plus local uncommitted edits to `src/scripts/scene.js`, `shaders.js`, and `quality.js`. Not served, not part of the live build.
